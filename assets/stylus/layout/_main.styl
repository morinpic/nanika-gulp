/*
 * main
 */

.lyMain {
  float: left;
  width: 720px;
  padding: 10px;
  min-height: 600px;
  box-sizing(border-box);
  background-color: #ccc;
  .headingMain {
    font-size: 24px;
    margin-bottom: 10px;
  }
  p {
    line-height: 24px;
  }
  .postMeta {
    margin-bottom: 20px;
  }
  .postContent {
    .headingContent {
      font-size: 20px;
      margin-bottom: 5px;
      padding-bottom: 5px;
      border-bottom: solid 1px #fff;
    }
  }
}